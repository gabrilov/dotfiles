# Alias
alias ls="lsd --group-directories-first"
alias ll="lsd -lh --group-directories-first"
alias lla="lsd -lah --group-directories-first"
alias grep="grep --color=auto"
alias egrep="egrep --color=auto"
alias instalar="sudo pacman -S"
alias arriba="sudo systemctl start"
alias abajo="sudo systemctl stop"
alias estado="sudo systemctl status"
alias buscar="find . -iname"
alias procid="xprop _NET_WM_PID | cut -d' ' -f3"
#alias vi="nvim"
alias vim="nvim"
alias cat="bat"
alias dotdrop='~/.dotfiles/dotdrop.sh'
mkcd () {
    mkdir -p $1
    cd $1
}
alias act="sudo pamac update"
# Linux version of OSX pbcopy and pbpaste.
alias pbcopy='xclip -selection clipboard'
alias pbpaste='xclip -selection clipboard -o'
alias ip='ip -c'
alias k='kubectl'
alias proy='cd /home/gabi/Documentos/ASIR/PROY/PROY03'
alias damdev='cd /home/gabi/Desarrollo/DAM/'
alias dam='cd /home/gabi/Documentos/DAM/'
[ "$TERM" = "xterm-kitty" ] && alias ssh="kitty +kitten ssh"
gitall() {
	git add . && \
	git commit -m $1 && \
	git push
}
alias aur="yay -Syua"
alias arch="sudo pacman -Syu"
function space() {
    local dir="${1:-.}"  # Si no se proporciona el primer argumento, usa el directorio actual (".")
    local num="${2:-10}"  # Si no se proporciona el segundo argumento, usa 10 como valor predeterminado
    du -hd 1 "$dir" 2>/dev/null | sort -rh | head -n "$num"
}
alias sudo="sudo "
alias busca="pacman -Ss"
alias buscaur="yay -Ssa"
